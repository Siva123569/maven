name: Maven Build Docker Push

on:
  push:
    branches:
      - feature
  workflow_dispatch:

jobs:
  build:
    runs-on: [self-hosted, Linux, X64]

    steps:
      - name: Checkout Code
        uses: actions/checkout@v4

      - name: Set up JDK 17
        uses: actions/setup-java@v4
        with:
          distribution: 'temurin'
          java-version: '17'

      - name: Build Maven Project
        working-directory: my-app
        run: mvn clean package

      - name: Docker Login
        run: echo "${{ secrets.DOCKER_PASSWORD }}" | docker login -u "${{ secrets.DOCKER_USERNAME }}" --password-stdin

      - name: Build Docker Image
        run: docker build -t ${{ secrets.DOCKER_USERNAME }}/varam-lakshimi:latest ./my-app

      - name: Push Docker Image
        run: docker push ${{ secrets.DOCKER_USERNAME }}/varam-lakshimi:latest

      - name: Run Docker Container
        run: |
          docker rm -f siva-container || true
          docker run -d -p 8080:8080 --name siva-container ${{ secrets.DOCKER_USERNAME }}/varam-lakshimi:latest
          docker ps
